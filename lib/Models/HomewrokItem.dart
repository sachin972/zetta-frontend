// ignore_for_file: file_names

class HomeworkItem {
  final String title;
  final String subject;
  final bool completed;
  final String? due;

  HomeworkItem(this.title, this.subject, this.completed, [this.due]);
}
